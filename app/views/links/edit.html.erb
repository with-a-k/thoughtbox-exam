<%= form_for @link, url: { action: 'update' } do |f| %>
	<%= f.label :title, "Title:" %>
	<%= f.text_field :title %><br>
	<%= f.label :url, "Location:" %>
	<%= f.text_field :url %><br>
	<%= f.submit "Save Changes" %>
<% end %>